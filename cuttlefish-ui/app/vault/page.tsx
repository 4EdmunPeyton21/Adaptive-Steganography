import VaultPage from "@/components/VaultPage";

export const metadata = {
  title: "Memory Vault · Cuttlefish",
  description: "Your secure, encrypted steganographic memory vault.",
};

export default function VaultRoute() {
  return <VaultPage />;
}
